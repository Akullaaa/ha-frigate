# Runtime databases и служебные файлы Frigate — не версионируем
*.db
*.db-shm
*.db-wal
.jwt_secret
.exports
.timeline
.search_stats.json
.vacuum
model_cache/
__pycache__/
go2rtc-tv-bin

# Runtime-история чата Ксоникса (xonix_chat_hub.py) и счёт побед
# (xonix_game.py) — пересоздаются с нуля, не версионируем, как и остальное
# runtime-состояние выше
xonix_chat_history.json
xonix_wins.json
xonix_llm_cost.json
