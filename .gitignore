# Runtime databases и служебные файлы Frigate — не версионируем
*.db
*.db-shm
*.db-wal
.jwt_secret
.exports
.timeline
.search_stats.json
.vacuum
model_cache/
__pycache__/
go2rtc-tv-bin

# Runtime-история чата Ксоникса (xonix_chat_hub.py) и счёт побед
# (xonix_game.py) — пересоздаются с нуля, не версионируем, как и остальное
# runtime-состояние выше
xonix_chat_history.json
xonix_wins.json
xonix_llm_cost.json
xonix_chat_alena.pid

# рантайм Frigate, не версионировать
backup_config.yaml
.runtime_state.json.lock
.runtime_state.json
logs/
xm530_dvrip_pause
frigate.db.bak*

# ручные бэкапы конфига (пароли, не шифруются)
config.yaml.*

# 24.09: библиотеки EGL и pip-пакеты для порта Авроры (бинарные, перекачиваются)
aurora/debs/
aurora/sysroot/
aurora/pylib/
