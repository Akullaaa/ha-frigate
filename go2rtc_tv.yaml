# Отдельный, изолированный экземпляр go2rtc — только для пользователя "tv"
# (Windows-обои на dellaxyea, второй монитор). Знает только ОДИН поток —
# вытягивает его у основного go2rtc этого же контейнера через 127.0.0.1
# (настоящий localhost — авторизация основного go2rtc для таких подключений
# всегда отключена, пароль alena тут не нужен). Отдаёт наружу под отдельным
# логином, полностью изолированным от доступа к остальным камерам/потокам.
# См. device_credentials/frigate_go2rtc_tv.txt (репозиторий ha) — там креды,
# порт, и как перезапускать при необходимости.
streams:
  tv:
    - rtsp://127.0.0.1:8554/dvor_xonix_nested
rtsp:
  listen: ":1935"
  username: tv
  password: dsuK7jJCeDmBs92AyE7l
api:
  listen: ":1994"
webrtc:
  listen: ""

